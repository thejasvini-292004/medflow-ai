# --- Core agent stack ---
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.40

# --- Vector store + embeddings ---
# The protocol index uses LangChain's built-in InMemoryVectorStore (ships with
# langchain-core), so there's no chromadb / native / sqlite3 dependency — this
# keeps memory low and deploys cleanly on constrained hosts (Streamlit Cloud).
# Optional local embeddings (uncomment if you want offline HF embeddings):
# sentence-transformers>=3.0.0
# langchain-huggingface>=0.1.0

# --- Data / DB ---
pandas>=2.1.0
SQLAlchemy>=2.0.0

# --- App + config ---
streamlit>=1.36.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
requests>=2.32.0

# --- Dev / test ---
pytest>=8.2.0
